<!DOCTYPE html>
<html lang="en" dir="ltr">

  <head>
    <meta charset="utf-8">
    <title>Choose cards</title>
  </head>

  <body>
    <h1>Choose label(s)</h1>
    <form class="label" action="/dates" method="get">
      {% for val in result %}
        <div>
          <p>{{val}}</p>
          <input type="checkbox" name="labels" value='{{val}}'>
        <div>
      {% endfor %}
    </form>
    <input type="submit" name="" value="Next">
  </body>

</html>
